#ifndef SHEL_L
#define SHEL_L

#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>

int _putchar(char c);
int print_string(char *string);
char **split_string(char * string, char * delim);

#endif /* SHEL_L */
